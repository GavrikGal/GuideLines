# from django import template
#
#
# register = template.Library()
#
#
# @register.filter
# def not_draft_or_not_author(value, user):
#     return value.filter(draft=False) or value.filter(author=user)

